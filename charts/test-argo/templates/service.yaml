apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-deploy-helm.fullname" . }}
  labels:
    {{- include "test-deploy-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "test-deploy-helm.selectorLabels" . | nindent 4 }}
